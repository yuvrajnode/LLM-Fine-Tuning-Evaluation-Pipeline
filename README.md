# LLM Fine-Tuning & Evaluation Pipeline

Parameter-efficient fine-tuning (LoRA/PEFT) plus RLHF preference optimisation for
open-source LLMs, with an automated harness that benchmarks every checkpoint the
training run produces.

Full docs to follow — this commit is the project skeleton.
